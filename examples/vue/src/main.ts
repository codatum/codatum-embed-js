/**
 * Vue example for @codatum/embed-vue.
 * Start examples/server and set config.json before running.
 */

import { createApp } from "vue";
import App from "./App.vue";

createApp(App).mount("#app");
